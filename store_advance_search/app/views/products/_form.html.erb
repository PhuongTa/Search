<%= form_for @product do |f| %>
	<%= f.collection_select :category_id, Category.all,:id,:name %>
	<%= f.label :name %>
	<%= f.text_field :name %>
	<%= f.label :price %>
	<%= f.text_field :price %>

	<%= f.submit "Save" %>
<% end %>